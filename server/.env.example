# ==========================
# Server Environment Variables
# ==========================

# Port to run the server on (default: 3001)
PORT=3001

# Origin of your React dev server (Vite default: 5173)
CLIENT_ORIGIN=http://localhost:5173

# ── Groq Provider ──────────────────────────────────────
# Get your key at https://console.groq.com/keys
GROQ_API_KEY=

# ── OpenAI Provider ────────────────────────────────────
# Get your key at https://platform.openai.com/api-keys
OPENAI_API_KEY=
# Set to "true" to allow the local provider to call private/loopback addresses.
# Required when running Ollama (localhost:11434) or LM Studio (localhost:1234) locally.
# NEVER set this to true in production.
ALLOW_LOCAL_ENDPOINTS=false
# ── Local LLM Connection (displayed in UI) ──────────────────────────────
# Endpoint URL of your local LLM server (Ollama, LM Studio, etc.)
LOCAL_LLM_ENDPOINT=http://localhost:11435
# Model name to use (leave blank to let the server decide)
LOCAL_LLM_MODEL=genera
# API key if your local server requires one (leave blank if not needed)
LOCAL_LLM_API_KEY=
# Node environment
NODE_ENV=development
